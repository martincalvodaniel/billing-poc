# Future Development

- Add query params to GET /api/payments (type, dateFrom, etc.) with validation
- Pagination: skip/limit or cursor; return metadata
- Authentication: integrate auth (e.g., NextAuth/Auth0); session validation; user-scoped payments
- Export endpoints: /api/payments/export for CSV/PDF using lightweight libs
